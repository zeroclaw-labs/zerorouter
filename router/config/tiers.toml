schema_version = 1

# This file is the sole source of truth for the public catalog, fallback order,
# upstream model IDs, and metering rates. Prices were verified on 2026-07-11.
#
# Pricing invariant, enforced at load time by `validate_tier_catalog`
# (`src/config.rs`): no candidate's cost basis may exceed its owning tier's
# sell rate on any dimension. Each tier's flagship candidate is priced *at* the
# tier sell rate (0% markup) and margin comes from routing inside the tier to
# cheaper candidates, so basis == sell is the intended flagship shape and stays
# legal; only a strictly greater basis is rejected, because such a candidate
# loses money on every request it serves. A candidate that a tier cannot cover
# belongs in a tier whose sell rates are at or above its basis, not here.

[tiers."zero/low-cost"]

[tiers."zero/low-cost".rates]
input_per_mtok = 0.30
cached_input_per_mtok = 0.06
output_per_mtok = 1.20

[[tiers."zero/low-cost".candidates]]
id = "fireworks/accounts/fireworks/models/minimax-m3"
provider = "fireworks"
model = "accounts/fireworks/models/minimax-m3"

[tiers."zero/low-cost".candidates.rates]
input_per_mtok = 0.30
cached_input_per_mtok = 0.06
output_per_mtok = 1.20

[[tiers."zero/low-cost".candidates]]
id = "together/MiniMaxAI/MiniMax-M3"
provider = "together"
model = "MiniMaxAI/MiniMax-M3"

[tiers."zero/low-cost".candidates.rates]
input_per_mtok = 0.30
cached_input_per_mtok = 0.06
output_per_mtok = 1.20

# MiniMax first-party (platform.minimax.io, OpenAI-compatible surface).
# Priced at the M2-era first-party rate; RE-VERIFY against the platform's M3
# price card before enabling MINIMAX_API_KEY in production — the rung is
# inert until that credential is set. Grouped with its aggregator twins so
# the same model is the walk's immediate same-quality failover; the western
# hosts stay ahead of bedrock per the note below. No cached-input rate is
# published for the first-party surface, so none is claimed.
[[tiers."zero/low-cost".candidates]]
id = "minimax/MiniMax-M3"
provider = "minimax"
model = "MiniMax-M3"

[tiers."zero/low-cost".candidates.rates]
input_per_mtok = 0.30
output_per_mtok = 1.20

[[tiers."zero/low-cost".candidates]]
id = "deepinfra/deepseek-ai/DeepSeek-V4-Flash"
provider = "deepinfra"
model = "deepseek-ai/DeepSeek-V4-Flash"

[tiers."zero/low-cost".candidates.rates]
input_per_mtok = 0.09
cached_input_per_mtok = 0.018
output_per_mtok = 0.18

# Bedrock-only private-beta fallback. Keep western OpenAI-compatible hosts
# ahead of it when their credentials are enabled.
[[tiers."zero/low-cost".candidates]]
id = "bedrock/minimax.minimax-m2.5"
provider = "bedrock"
model = "minimax.minimax-m2.5"

[tiers."zero/low-cost".candidates.rates]
input_per_mtok = 0.30
output_per_mtok = 1.20

[tiers."zero/balanced"]

[tiers."zero/balanced".rates]
input_per_mtok = 1.74
cached_input_per_mtok = 0.14
output_per_mtok = 3.48

[[tiers."zero/balanced".candidates]]
id = "fireworks/accounts/fireworks/models/deepseek-v4-pro"
provider = "fireworks"
model = "accounts/fireworks/models/deepseek-v4-pro"

[tiers."zero/balanced".candidates.rates]
input_per_mtok = 1.74
cached_input_per_mtok = 0.14
output_per_mtok = 3.48

[[tiers."zero/balanced".candidates]]
id = "deepinfra/deepseek-ai/DeepSeek-V4-Pro"
provider = "deepinfra"
model = "deepseek-ai/DeepSeek-V4-Pro"

[tiers."zero/balanced".candidates.rates]
input_per_mtok = 1.30
cached_input_per_mtok = 0.10
output_per_mtok = 2.60

# OpenAI first-party on ZeroRouter's OWN Responses wire client
# (`src/wire.rs`, the owned-wire decision): the pinned adapters cannot
# serve gpt-5.x — the chat wire rejects the family and the pinned Responses
# provider discards usage (the three-layer investigation of 2026-07-31,
# preserved in this file's history). gpt-5.6-luna is the 5.6 family's small
# tier, price-verified 2026-07-31 (0.20/0.02/1.20): newer AND cheaper than
# the gpt-4.1-mini rung it replaces. Inert until OPENAI_API_KEY is set.
[[tiers."zero/balanced".candidates]]
id = "openai/gpt-5.6-luna"
provider = "openai"
model = "gpt-5.6-luna"

[tiers."zero/balanced".candidates.rates]
input_per_mtok = 0.20
cached_input_per_mtok = 0.02
output_per_mtok = 1.20

# Bedrock-only private-beta fallback. This is priced at the canonical Bedrock
# us-east-1 on-demand rate while the tier keeps its existing sell rate.
[[tiers."zero/balanced".candidates]]
id = "bedrock/deepseek.v3.2"
provider = "bedrock"
model = "deepseek.v3.2"

[tiers."zero/balanced".candidates.rates]
input_per_mtok = 0.62
output_per_mtok = 1.85

[tiers."zero/high-end"]

# SELLS at Sonnet 5's post-intro standard rate (3.00/0.30/15.00, effective
# upstream 2026-09-01); BASES at the introductory rate we actually pay
# through 2026-08-31. The gap is deliberate margin for the remainder of the
# intro window, and it defuses the scheduled cliff this comment used to
# warn about: on 2026-09-01 the only required edit is raising both sonnet
# BASIS blocks to 3.00/0.30/15.00 — which equals the sell (flagship shape)
# and therefore cannot trip the margin withhold no matter when the edit
# lands relative to the date.
[tiers."zero/high-end".rates]
input_per_mtok = 3.00
cached_input_per_mtok = 0.30
output_per_mtok = 15.00

[[tiers."zero/high-end".candidates]]
id = "anthropic/claude-sonnet-5"
provider = "anthropic"
model = "claude-sonnet-5"

[tiers."zero/high-end".candidates.rates]
input_per_mtok = 2.00
cached_input_per_mtok = 0.20
output_per_mtok = 10.00

[[tiers."zero/high-end".candidates]]
id = "bedrock/us.anthropic.claude-sonnet-5"
provider = "bedrock"
# Bedrock exposes Sonnet 5 through an inference profile, not direct invocation.
model = "us.anthropic.claude-sonnet-5"

[tiers."zero/high-end".candidates.rates]
input_per_mtok = 2.00
cached_input_per_mtok = 0.20
output_per_mtok = 10.00

# ---------------------------------------------------------------------------
# Pass-through catalog: one single-candidate tier per model, sell = verified
# provider list price (flagship shape, 0% markup — legal by the margin rule).
# This is the second pricing model beside the routed zero/* tiers: customers
# who want a SPECIFIC newest model select it here at list; margin on these
# rows arrives when provider volume discounts lower our basis below the
# unchanged sell, with no schema change — the per-row
# cost_usd − cost_basis_usd ledger shows exactly when that flips. Routed
# tiers remain the premium abstraction (margin from routing; later, fees).
# Prices verified 2026-07-31 on platform.claude.com/docs pricing; cache-read
# rates are the provider's "cache hits" column (0.1 × input).
# ---------------------------------------------------------------------------

[tiers."zero/opus-5"]
[tiers."zero/opus-5".rates]
input_per_mtok = 5.00
cached_input_per_mtok = 0.50
output_per_mtok = 25.00
[[tiers."zero/opus-5".candidates]]
id = "anthropic/claude-opus-5"
provider = "anthropic"
model = "claude-opus-5"
[tiers."zero/opus-5".candidates.rates]
input_per_mtok = 5.00
cached_input_per_mtok = 0.50
output_per_mtok = 25.00

[tiers."zero/fable-5"]
[tiers."zero/fable-5".rates]
input_per_mtok = 10.00
cached_input_per_mtok = 1.00
output_per_mtok = 50.00
[[tiers."zero/fable-5".candidates]]
id = "anthropic/claude-fable-5"
provider = "anthropic"
model = "claude-fable-5"
[tiers."zero/fable-5".candidates.rates]
input_per_mtok = 10.00
cached_input_per_mtok = 1.00
output_per_mtok = 50.00

[tiers."zero/sol"]
[tiers."zero/sol".rates]
input_per_mtok = 5.00
cached_input_per_mtok = 0.50
output_per_mtok = 30.00
[[tiers."zero/sol".candidates]]
id = "openai/gpt-5.6-sol"
provider = "openai"
model = "gpt-5.6-sol"
[tiers."zero/sol".candidates.rates]
input_per_mtok = 5.00
cached_input_per_mtok = 0.50
output_per_mtok = 30.00

[tiers."zero/codex"]
[tiers."zero/codex".rates]
input_per_mtok = 1.75
cached_input_per_mtok = 0.175
output_per_mtok = 14.00
[[tiers."zero/codex".candidates]]
id = "openai/gpt-5.3-codex"
provider = "openai"
model = "gpt-5.3-codex"
[tiers."zero/codex".candidates.rates]
input_per_mtok = 1.75
cached_input_per_mtok = 0.175
output_per_mtok = 14.00

[tiers."zero/haiku-4-5"]
[tiers."zero/haiku-4-5".rates]
input_per_mtok = 1.00
cached_input_per_mtok = 0.10
output_per_mtok = 5.00
[[tiers."zero/haiku-4-5".candidates]]
id = "anthropic/claude-haiku-4-5"
provider = "anthropic"
model = "claude-haiku-4-5-20251001"
[tiers."zero/haiku-4-5".candidates.rates]
input_per_mtok = 1.00
cached_input_per_mtok = 0.10
output_per_mtok = 5.00
